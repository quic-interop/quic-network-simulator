version: "3.7"

services:
  server:
    volumes:
      - $WWW:/www
    environment:
      - TESTCASE=$TESTCASE
  client:
    volumes:
      - $DOWNLOADS:/downloads
    environment:
      - TESTCASE=$TESTCASE
